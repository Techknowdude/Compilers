{
    float a;
    int b;
    int Add(int x)
    {
        int next;
        if(x)
            next = Add(x - 1);
        
        return next;
    }
    
    b = 2;
    a = Add(b + 2);
    print(a);
}
