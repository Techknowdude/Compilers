
offsets for block

offset = base

offset = decl->compute(offset)
offset = stmt->compute(offset)

size = offset - base

return base



offset for decls

offset = base
loop
begin
offset = thing->compute(offset)
end

size = offset - base

ret offset


offset for vardecl

// check for word align
 if size >= word_size
    //allign it


refs do not take space, just compute the offset for the members
